# store the AWS region as string
region = "us-east-1"
# store the availability zone as a string
az = "us-east-1a"
# store the instance count as an integer
instance_count = 3
# print all three on one line using an f-string
print(f"Region: {region} | AZ: {az} | Instances: {instance_count}")
